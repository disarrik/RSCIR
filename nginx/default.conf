server {
    listen       80;       
    listen  [::]:80;       
    server_name  localhost;

    #access_log  /var/log/nginx/host.access.log  main;

    location /pages {
        root   /data/app;
    }

    location / {
        proxy_pass http://server;
    }
}